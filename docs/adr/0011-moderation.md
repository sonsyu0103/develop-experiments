# ADR 0011: モデレーション —— ロール・削除権限・通報

- ステータス: **採用・実装済み**。決定 1 は前半 (ロール) と後半 (ロール変更 API)、
  決定 2〜5 は後半で実装。管理画面 (Next.js) も入った (2026-08-16)
- 日付: 2026-08-05

## 背景

[ADR 0005](0005-authentication.md) の権限モデルには**管理者が存在しない**。
その結果、次の状態になっていた。

| 状況 | 現状の権限モデルでの結末 |
| --- | --- |
| 匿名で荒らし投稿がされた | `author_id` が NULL なので **誰も削除できない** |
| ログインユーザーが不適切な画像を投稿した | 本人しか消せない。本人は消さない |
| なりすまし・誹謗中傷の投稿 | 対応手段がない |

画像投稿を許し ([ADR 0007](0007-image-storage.md))、
100 万ユーザー規模を想定する ([ADR 0009](0009-scaling-strategy.md)) 以上、
この状態では運用できない。

## 決定 1: ロールは `users` の列として持つ

```sql
ALTER TABLE users ADD COLUMN role TEXT NOT NULL DEFAULT 'user';
ALTER TABLE users ADD CONSTRAINT users_role_valid
    CHECK (role IN ('user', 'moderator', 'admin'));
```

別テーブル (`user_roles`) にしないのは、**1 ユーザーが持つロールは 1 つ**であり、
多対多にする理由がないため。権限の組み合わせを細かく制御する要求が出たら、
そのときに切り出す。

3 段階に分けるのは、**投稿を消せる権限と、権限を配れる権限を分離する**ため。

| | `user` | `moderator` | `admin` |
| --- | --- | --- | --- |
| 自分の投稿の削除 | ✅ | ✅ | ✅ |
| **他人・匿名の投稿の削除** | ❌ | ✅ | ✅ |
| 通報キューの閲覧 | ❌ | ✅ | ✅ |
| **ロールの変更** | ❌ | ❌ | ✅ |

モデレーターを増やしても、権限を配れる人間は増えない。

### 権限昇格を作り込まない

- **ユーザー登録時にリクエストから `role` を読まない。** 常に `'user'` 固定。
  「クライアントから送られた値を保存する」経路を 1 か所でも作ると、
  そこが権限昇格の入口になる
- ロール変更 API は `admin` のみ。変更は監査記録に残す (決定 3)

### 最初の管理者をどう作るか

**UI からは作れないようにする。** 「最初の 1 人」を作る機能は、
そのまま「誰でも管理者になれる」機能になりうる。

環境変数 `BOOTSTRAP_ADMIN_GOOGLE_SUB` に Google の `sub` を指定し、
その値に一致するユーザーがログインしたときだけ `admin` を付与する。

シークレット扱いの値ではないが、
[ADR 0005](0005-authentication.md) の他の設定と同じく
Secrets Manager から渡す ([インフラ構成](../infrastructure.md))。

## 決定 2: 削除はすべて論理削除。匿名投稿もモデレーターは消せる

既存の `SoftDelete` をそのまま使う。物理削除しない理由は 2 つある。

- **誤って消したときに戻せる。** モデレーションは判断を伴うので必ず誤操作が起きる
- コメントの参照整合性が保たれる (既存のスキーマ設計の理由と同じ)

匿名投稿 (`author_id IS NULL`) は、**本人には消せないがモデレーターには消せる**。
[ADR 0005](0005-authentication.md) の「匿名投稿は誰も削除できない」を
この ADR で上書きする。

## 決定 3: モデレーション操作は DB に記録する

```sql
CREATE TABLE moderation_actions (
    id          BIGINT      GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    actor_id    BIGINT      NOT NULL REFERENCES users(id),
    action      TEXT        NOT NULL,   -- 'delete_thread' | 'delete_comment' | 'delete_image' | 'change_role'
    target_type TEXT        NOT NULL,   -- 'thread' | 'comment' | 'image' | 'user'
    target_id   TEXT        NOT NULL,   -- 型が混在するため TEXT
    reason      TEXT,
    created_at  TIMESTAMPTZ NOT NULL DEFAULT now(),

    CONSTRAINT moderation_action_valid CHECK (
        action IN ('delete_thread', 'delete_comment', 'delete_image', 'change_role')
    )
);

CREATE INDEX moderation_actions_actor_idx  ON moderation_actions (actor_id, created_at DESC);
CREATE INDEX moderation_actions_target_idx ON moderation_actions (target_type, target_id);
```

**ログ ([ADR 0010](0010-log-pipeline.md)) ではなく DB に書く。**

ADR 0010 で「ログは at-most-once であり欠落しうる。
課金・監査に使う数値をログの件数から数えない」と決めた。
モデレーション記録はまさに監査であり、
「誰が消したか分からない」状態は許容できない。

ログにも出すが、**正はこのテーブル**になる。

## 決定 4: 通報はログイン必須。閾値による自動削除はしない

```sql
CREATE TABLE reports (
    id           BIGINT      GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    reporter_id  BIGINT      NOT NULL REFERENCES users(id),
    target_type  TEXT        NOT NULL,   -- 'thread' | 'comment'
    target_id    BIGINT      NOT NULL,
    reason       TEXT        NOT NULL,   -- 'spam' | 'abuse' | 'illegal' | 'other'
    note         TEXT,
    status       TEXT        NOT NULL DEFAULT 'open',  -- 'open' | 'resolved' | 'rejected'
    created_at   TIMESTAMPTZ NOT NULL DEFAULT now(),
    resolved_at  TIMESTAMPTZ,
    resolved_by  BIGINT      REFERENCES users(id),

    CONSTRAINT reports_reason_valid CHECK (reason IN ('spam','abuse','illegal','other')),
    CONSTRAINT reports_status_valid CHECK (status IN ('open','resolved','rejected')),
    -- 同一ユーザーによる同一対象への重複通報を防ぐ
    CONSTRAINT reports_unique_per_user UNIQUE (reporter_id, target_type, target_id)
);

CREATE INDEX reports_open_idx ON reports (created_at) WHERE status = 'open';
```

### 匿名通報を許さない

通報を匿名で受け付けると、**通報そのものが荒らしの手段**になる。
無限に通報を投げてキューを埋められる。

投稿は匿名を許すが ([ADR 0005](0005-authentication.md))、
通報はログイン必須とする。非対称だが、
**投稿は表現、通報は他人への申し立て**であり、後者には責任の所在が要る。

### 通報数が閾値を超えても自動で消さない

「N 件通報されたら自動的に非表示」は実装が簡単で、しかも**危険**になる。

- 組織的に通報を集めれば、正常な投稿を消せる (通報爆撃)
- 少数派の意見が構造的に消えやすくなる

通報は**モデレーターのキューに積むだけ**にする。
判断は必ず人間が行い、その結果が `moderation_actions` に残る。

自動化するとしても、上限は「優先度を上げる」までとする。

## 決定 5: 削除された画像はストレージからも消す

論理削除では S3 のオブジェクトが残るため、
**不適切な画像は URL を直接叩けば見え続ける**。
CloudFront にキャッシュも残る。

[ADR 0007](0007-image-storage.md) の `images.status` に `'deleted'` を追加する。

```
モデレーターが画像を削除
   → images.status = 'deleted' に更新 (投稿本体は論理削除)
   → 回収バッチが S3 のオブジェクトを削除
   → CloudFront の該当パスを invalidate
```

回収バッチは既に `'pending'` の孤児を回収する役目を持っているので、
**対象に `'deleted'` を加えるだけ**で済む。定期処理は増えない。

CloudFront の invalidate を忘れると、削除後もキャッシュ期間中は配信され続ける。

## モジュール構成: `moderation` を独立させる

[ADR 0004](0004-modular-monolith.md) に従い `internal/moderation` を追加する。

通報とモデレーション記録は `thread` / `comment` / `image` の
いずれにも属さない**横断的な関心事**であり、
どれかに寄せると他の 2 つへの参照が必要になってモジュール境界が壊れる。

削除操作は、各モジュールが公開する最小のインターフェース
(`ThreadSoftDeleter` など) を `moderation` 側で定義して受ける。

### ただし、これで depguard が 15 ルールになる

モジュールは 6 つ (`thread` / `comment` / `user` / `image` / `contact` / `moderation`)。
1 モジュールにつき 2 ルール + レイヤ用 3 ルールで **15 ルール**。

[ADR 0004](0004-modular-monolith.md) が
「9 を超えて破綻するようなら `go-arch-lint` を再検討する」と書いた水準を、
明確に超える。

**[ADR 0003](0003-open-questions.md) の未決 #13 の判断が、
この ADR で先送りできなくなった。**

なお、そこで数え直したところ**問題はルール数ではなかった**。
`deny` エントリが約 106 になること、そして
**書き忘れが「許可」に倒れる**ことが本体になる。

> **その後の決定 ([ADR 0017](0017-arch-lint-and-depguard.md))**
> 実地検証の結果、検査は `depguard` から `go-arch-lint` に移した。
> `depguard` のルールは 0 になり、この節が心配していた 15 ルールは発生しない。
> `moderation` に必要なのは、2 枚の arch ファイルへの約 16 行ずつの宣言になる。
> なお未決 #13 は穴の所在を「拒否リストであること」としていたが、
> 実測では**どのルールにも一致しないファイルが無検査になること**だった。

## 引き受けるコスト

- **管理画面が要る。** 通報キュー、投稿の削除、ロール変更。
  Next.js 側の作業が増え、しかも**認証の 4 層キャッシュ問題
  ([ADR 0005](0005-authentication.md)) が最も厳しく効く画面**になる。
  管理者向けの内容が他人にキャッシュされたら事故では済まない
- ~~**depguard が 15 ルールになる**~~ ——
  [ADR 0017](0017-arch-lint-and-depguard.md) で検査を `go-arch-lint` に移したため、
  arch ファイル 2 枚に約 16 行ずつの宣言で済む
- **モデレーターの誤操作は戻せるが、その導線も要る。**
  論理削除なので復活は可能だが、「復活させる UI」がなければ
  DB を直接触ることになる
- **通報の一意制約が UX に影響する。**
  同じ対象に 2 回通報しようとするとエラーになる。
  「既に通報済みです」として正常に扱う必要がある
- **`target_id` を `TEXT` にしている。** `images.id` が UUID、
  `threads` / `comments` が `BIGINT` で型が混在するため。
  [ADR 0003](0003-open-questions.md) の #11 は
  **用途ごとに使い分ける (統一しない)** 決定になったので、
  この混在は解消されない。`TEXT` のままとする。
  型が違うものを 1 列に詰めている以上、
  **参照整合性は外部キーで保証できない**点に注意する
  (削除済みの対象を指す行が残りうる)

## 実装して分かったこと

決定 2・3・5 を実装した時点 (`000008` / `internal/moderation`) の記録。

### 1. 決定 5 には 1 時間の穴が空いていた

この ADR は「削除された画像はストレージからも消す」理由を
**「URL を直接叩けば見え続ける」**ことに置いている。
ところが回収バッチのクエリ (`ListReclaimableImages`) は、
対象 3 種類のすべてに `created_at < now() - grace` の足切りをかけていた。

猶予は「アップロードしたが、まだ添付していない」画像を守るためのもので、
**`deleted` には要らない。** 削除は明示的な操作であり、
「まだ判断がついていない」状態が存在しない。

結果として、投稿直後の不適切な画像をモデレーターが削除しても、
**作成から 1 時間が経つまで S3 に残り続けていた。**
決定 5 が消そうとしたものが、いちばん消したい時間帯にそのまま残る形になる。

猶予の条件を `OR` の内側へ移して直した。
`images_reclaimable_idx` の述語は 1 文字も変えていない ——
索引に載る条件と、載った行から絞る条件は別物であり、
**触ったのは後者だけ**になる。

> **なぜ Phase 6 で気づけなかったか。**
> `deleted` を書く経路が無かったため、回収の検査はすべて
> `UPDATE images SET status='deleted'` を SQL で直接書いて作った状態から
> 始めていた。そのとき `created_at` は「2 時間前」に寄せてあり、
> **猶予の内側にある `deleted`** という状態が 1 度も作られていない。
> 引き継ぎ文書が「`deleted` の経路はまだ本番で一度も動いていない」と
> 名指しした箇所が、そのまま穴だった。

### 2. コメントの削除にはスレッド ID が要る

`comments` は `thread_id` による HASH パーティションで、主キーが
`(thread_id, id)` になる。**コメント ID だけでは先頭列を絞れないため、
8 パーティションすべてを走査する。**

そのため `POST /moderation/actions` は、`delete_comment` のときだけ
`threadId` を追加で受け取る。「対象は `targetId` ひとつ」という
きれいな形にはならなかったが、パーティション構成が API の形に
にじみ出るのは避けられない。

> **記録の側にも残す必要があった** (レビュー指摘)。
> 初版は `moderation_actions.target_id` にコメント ID だけを書いていた。
> **削除するときは `thread_id` が要ると言いながら、
> 「何を削除したかの記録」からは落ちている**状態になる。
>
> `moderation_actions_target_idx (target_type, target_id)` から辿った先で、
> この決定が避けたはずの 8 パーティション全走査が必要になる。
> しかも復活や調査は記録を起点に始まるので、**運用のたびに起きる。**
>
> `target_id` を `"{thread_id}:{comment_id}"` にした。
> 区切りの `:` は 10 進の整数にも UUID にも現れないため、
> 他の対象種別と取り違えようがない。
> BIGINT は最大 19 桁なので、区切りを含めても 39 文字で
> `moderation_target_id_length` (64) に収まる。
>
> 決定 3 が `target_id` を `TEXT` にしたのは型が混在するためだったが、
> **「複合キーを表せる」という副産物がここで効いた。**

### 3. 対象種別をリクエストから受け取らない

`moderation_actions` は `action` と `target_type` を別の列に持つが、
**API では `action` しか受け取らない。**
受け取ると `delete_thread` と `image` のように食い違う組み合わせを
表現でき、それがそのまま監査記録に残る。
`action` が決まれば対象種別は一意なので、導出する側に倒した。

### 4. `change_role` は同じテーブルに書くが、同じ API では受けない

決定 3 の `action` は 4 値だが、削除の API が公開する enum は 3 値になる。
ロール変更は入力の形が違う (新しいロールが要る) ため、
専用のエンドポイントが書く。**記録先のテーブルは同じ**なので、
決定 3 の「モデレーション操作は DB に記録する」は保たれる。

そのぶん、定義の突き合わせ検査は
「Go の定数 = DB の CHECK 制約 ⊃ 仕様書の enum」という
非対称な形になる (`action_test.go`)。

### 5. enum に値を 1 つ足すと、無関係な定数の名前が変わる

`ModerationTargetType` に `user` を足した時点で、
既存の `Role` enum (`user` / `moderator` / `admin`) と値が衝突し、
oapi-codegen が生成する定数名が `User` → `RoleUser` に変わった。

**仕様書に足したのは別のスキーマ**なのに、既存の定数名が動く。
生成物を追跡していれば `make verify-generated` が検出するが、
「スキーマを足しただけ」のつもりでいると原因に辿り着きにくい。

### 6. 決定 4 の DDL には 2 つ足りなかった

`reports` は 000008 で決定 4 の DDL をそのまま写して作ったが、
**使い始める段になって 2 つ足りないと分かった** (000009 で追加)。

**`target_thread_id` が無かった。** `target_id` が `BIGINT` なので、
コメントを指す通報には ID しか入らない。ところが `comments` は
`thread_id` による HASH パーティションで主キーが `(thread_id, id)` ——
**コメント ID だけでは本文にも辿り着けない。**
「通報された投稿を見て判断する」ための画面が、対象を引けなくなる。

`moderation_actions` は `target_id` が `TEXT` だったので `"1:10"` と
複合キーを詰めて解決したが、決定 4 は
「型が混ざらないので揃えて `TEXT` にはしない」と決めている。
**列を足すほうがその決定と噛み合う。**

**`reports_open_idx` が `created_at` だった。**
キューはキーセットページネーションで読むが、
**`created_at` は一意ではない** —— 同じ時刻の行が複数あるページ境界で
取りこぼしと重複が起きる。通報は荒らしへの対応という性質上、
短時間に集中して届くので実際に起きる。

`id` は IDENTITY で一意かつ単調増加、`created_at` の既定は `now()` なので、
**順序は実質同じで一意性だけが得られる。** 索引を `(id)` へ張り替えた。

> **キューだけカーソルの向きが逆になる。**
> 他の一覧は新しい順 (`id < cursor`) だが、キューは古い順 (`id > cursor`)。
> 取り違えると「次ページが常に空」になるので、
> 実 DB の検査とスモークの両方で向きを見ている。

### 7. 「自分を触らせない」だけでは admin ゼロを防げなかった

決定 1 は「ロール変更 API は admin のみ」としか書いていないが、
**最後の admin が降格すると誰もロールを配れなくなる。**
復旧には DB を直接触るか `BOOTSTRAP_ADMIN_GOOGLE_SUB` を
設定し直して再ログインするしかない。

初版は「自分自身を対象にできない」だけで塞いだつもりでいた。
数える形を避けたのは「数えた直後に他の admin が降格する競合がある」ためで、
自分を触らせないほうが単純で確実だと考えた。

> **足りなかった** (レビュー指摘)。admin が A と B の 2 人いるとき、
> **互いを同時に降格させると両方が通る。**
>
> - A のリクエストは対象が B なので自分自身チェックを抜ける
> - B のリクエストも対象が A なので抜ける
> - 更新する行が別なので**行ロックも衝突しない**
> - 権限判定はリクエスト開始時の principal に基づくので両方 admin
>
> 両方コミットして **admin が 0 人**になる。
>
> **数える形を避けた理由そのものが、正しい対処の形を示していた。**
> 競合が問題なら、競合しないように**ロックを取ればよい**。
>
> `SELECT ... WHERE role = 'admin' FOR UPDATE` で admin の行をすべて
> ロックしてから数える。同時に走った 2 つの降格は直列化され、
> 後から来たほうは先のコミット後に数え直して 1 人だと分かる。
> 昇格 (admin にする変更) では admin が減らないので、ロックは取らない。
>
> 弾くときは **422** にする —— 入力の形は正しく、
> 現在の状態と噛み合わないだけで、再試行しても解決しない
> (先に別の admin を作る必要がある)。
>
> **自分自身チェックは残す。** ロックの検査に先行して弾けるほうが、
> 「なぜ拒否されたか」が利用者に伝わりやすい (403 と 422 で分かれる)。

### 8. 監査記録の ID と API の ID は別物になる

`moderation_actions.target_id` には**内部 ID** を書く ——
監査記録から `users` や `threads` を辿るためで、
公開 ID だと結合がもう 1 段要る。

一方 **API は内部 ID を出さない** ([ADR 0003](0003-open-questions.md) 未決 #11)。

> **ロール変更の応答でこれが破れていた** (レビュー指摘)。
> 記録をそのまま返していたため、`targetId` に `users.id` が載っていた。
> 同じ PR が `reports.reporter_id` を隠して守っている方針に、
> ここだけ穴が空いていたことになる。
>
> 実害もある: 管理画面は受け取った `targetId` を他の API へ渡せない
> —— API が受け付けるのは `publicId` だけになる。
>
> 応答を組み立てるときに公開 ID へ詰め替えた。
> **記録と応答で同じ列が違う値になる**ので、仕様書に明記してある。

### 9. 処理済みの通報は、同じ人からは再申し立てできない

`reports_unique_per_user` は `status` を含まないため、
モデレーターが `resolved` / `rejected` にしたあとでも、
同じ利用者の同じ対象への通報は重複として 200 で返る ——
**キューには何も積まれない。**

決定 4 の一意制約をそのまま採ったことによる制限で、
「同じ人が同じ対象を何度も積む」ことを防ぐ側を優先している。
応答の `status` を見れば処理済みかは分かるので、仕様書に明記した。

投稿は編集できないので影響は限定的だが、
**スレッドが後から荒れた場合に再申し立てできない**のは残る。
塞ぐなら一意制約を部分索引 (`WHERE status = 'open'`) にする形になる。

### 10. 権限の真偽値を 1 つにまとめない

`Actor` は `CanModerate` と `CanChangeRoles` を別に持つ。
1 つにまとめると、決定 1 の趣旨
「投稿を消せる権限と、権限を配れる権限を分離する」がコード上で消え、
**モデレーターを増やすと権限を配れる人間も増える**形になる。

変異プローブの ② (`CanChangeRoles` を `CanModerate` に差し替える) が
これを実測している。**2 つに分かれていなければ、この変異は書けない。**

### 11. CloudFront の invalidate はまだ実装していない

決定 5 の手順の最後 (`CloudFront の該当パスを invalidate`) は入っていない。
ローカルと CI に CloudFront の対応物が無く
([インフラ構成](../infrastructure.md))、検証できないものを
書いても動作を確認できないため。

**削除の遅れは 2 段ある。**

| | 遅れ |
| --- | --- |
| S3 から消えるまで | 回収バッチの周回間隔 (既定 10 分) |
| CDN から消えるまで | **未実装。キャッシュ期間ぶん残る** |

前者は仕様書の `POST /moderation/actions` に明記した。
後者は本番構成を組む段階で塞ぐ必要がある。

## 実装して分かったこと (管理画面 / 2026-08-16)

「引き受けるコスト」に挙げた**管理画面が要る**を実装した時点の記録。

### 12. 4 層キャッシュ問題は、取得をブラウザへ寄せると消えた

このコストの本体は「**管理者向けの内容が他人にキャッシュされたら
事故では済まない**」ことだった。[ADR 0005](0005-authentication.md) が
確認すべき層を 4 つ挙げている。

ところが [ADR 0013](0013-http-defense.md) の 7 が既に
「書き込みはブラウザから直接叩く」を決めていて、
**それに従うと読み取りも同じ経路に揃えたくなる** ——
サーバ側から読むと Cookie が自動転送されず、
読みと書きで経路が分かれるほうが間違えやすいためになる。

揃えた結果、**サーバが返す HTML に利用者ごとの内容が 1 つも入らなくなった。**
残る層は `fetch` の 1 つだけになる。

**実測**: `next build` が `/admin` と `/admin/roles` を `○ (Static)` と判定した。

> **ただし、これは崩れやすい形になる。**
> 管理画面で `cookies()` を読んだ瞬間に 4 層へ戻る。
> 「サーバ側で認証を見て出し分ける」は自然な発想なので、
> 詳細は ADR 0005 の該当節に書いた。

### 13. 通報キューから、通報された投稿の中身が見えない

決定 4 は「判断は必ず人間が行う」としているが、
**その人間に見せるものが無かった。** 通報が持っているのは ID だけになる。

スレッドの通報は `GET /threads/{id}` でタイトルを引けるようにしたが、
**コメントの本文は出せない** —— コメントを 1 件だけ引く API が無く、
一覧を辿るしかないためになる。フロントにスレッド詳細の画面も無い
(今あるのは一覧 1 枚だけ)。

現状は「スレッドのタイトル + コメント ID」までを見せて、
**本文が未表示であることを画面に明記した。**
塞ぐには、スレッド詳細の画面か、コメントを 1 件引く経路が要る。

### 14. **投稿を消しても画像は残る。** 決定 5 に導線が無かった

決定 5 は「削除された画像はストレージからも消す」だが、
**そこへ至る操作が管理画面から辿れなかった。**

- 通報は画像を対象にできない (`ReportTargetType` は `thread` / `comment`)
- コメントを論理削除しても、画像は回収の対象にならない ——
  回収バッチが拾うのは「添付されていない」か「`status = 'deleted'`」で、
  **添付済みのまま論理削除されたコメントの画像はどちらでもない**

つまり `delete_image` を明示的に呼ばない限り、
**不適切な画像は S3 に残り、URL を直接叩けば見え続ける。**
決定 5 が消そうとしたのはまさにこの状態になる。

画像の削除だけ独立した入力欄を置いた。UUID をそのまま入れても、
画像の URL を貼っても通るようにしてある ——
モデレーターが手に持っているのは大抵 URL のほうになる。

> **決定 5 の穴は、これで 2 つ目になる。**
> 1 つ目は猶予期間の位置 (実装して分かったこと 1)、2 つ目が導線の欠落。
> どちらも「消す仕組みはあるが、消したい状態に届かない」形だった。

### 15. 画面でロールを見ても、それは防御ではない

管理画面は `Me.role` を見てリンクとボタンを出し分けるが、
**判定はサーバが行う。** 隠しても API を直接叩けば通り、
通しても API が 403 を返せば何もできない。

書いておかないと「画面で弾いているから」を理由に
サーバ側の検査を省く変更が入りうる。
変異プローブの ④ (ハンドラが `CanChangeRoles` を常に true にする) が、
**詰め替えの 1 行で全員が admin になる**ことを実測している。

なお 401 と 403 で導線を変えている ([ADR 0013](0013-http-defense.md) 決定 3)。
401 にはログインの導線を出し、**403 には出さない** ——
誰かは分かっている状態なので、ログインし直しても解決しないためになる。
