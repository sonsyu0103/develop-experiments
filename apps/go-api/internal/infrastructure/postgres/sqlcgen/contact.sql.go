// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: contact.sql

package sqlcgen

import (
	"context"
	"net/netip"
	"time"

	"github.com/jackc/pgx/v5/pgtype"
)

const claimPendingContacts = `-- name: ClaimPendingContacts :many
UPDATE contact_messages
SET attempt_count   = attempt_count + 1,
    next_attempt_at = now() + $1::interval
WHERE id IN (
    SELECT id FROM contact_messages
    WHERE status = 'pending'
      AND next_attempt_at <= now()
    ORDER BY next_attempt_at, id
    FOR UPDATE SKIP LOCKED
    LIMIT $2
)
RETURNING id, user_id, name, email, subject, body, attempt_count, created_at
`

type ClaimPendingContactsParams struct {
	Lease     pgtype.Interval
	BatchSize int32
}

type ClaimPendingContactsRow struct {
	ID           int64
	UserID       *int64
	Name         string
	Email        string
	Subject      string
	Body         string
	AttemptCount int32
	CreatedAt    time.Time
}

// 送信する行を確保する。**このリポジトリで唯一の SKIP LOCKED。**
//
// 【二重送信を防ぐ】
// ワーカーは API プロセスの中で動くので (ADR 0003 未決 #9)、
// **レプリカの数だけ同時に走る**。素朴に SELECT すると、
// 同じ問い合わせが 2 通届く。
//
// `FOR UPDATE SKIP LOCKED` は、他のトランザクションがロック中の行を
// **待たずに飛ばす**。各インスタンスは互いに異なる行を取得する。
//
//	SKIP LOCKED 無し → 解放を待ち、待った先で処理済みの行を読む
//	NOWAIT          → エラーになる (どちらも使えない)
//
// 【SELECT ではなく UPDATE にしている】
// ADR 0008 の図は「取り出す → 送信 → status 更新」を 1 つの流れで書いているが、
// そのまま 1 トランザクションにすると **SMTP の往復のあいだ行ロックを
// 保持し続ける**。外部サービスの応答時間だけ長いトランザクションが残り、
// プールの接続も 1 本占有する。
//
// そこで**確保だけを 1 文で終わらせて即コミットする**。
// 画像の回収 (ADR 0007 / image usecase の Reclaim) が
// 「確保 → S3 削除 → DB 反映」と 3 段階に割っているのと同じ形になる。
//
// 確保の中身は 2 つ:
//
//	attempt_count を先に増やす  クラッシュを繰り返す行が無限に居座らない
//	next_attempt_at を先送りする 送信中の行を他のインスタンスが拾わない
//	                            (= リース。送信が落ちてもリース切れで戻る)
//
// **これは at-least-once であって exactly-once ではない** (ADR 0008)。
// 送信の直後、status を書く前にプロセスが落ちると、リース切れの後に
// もう一度送られる。SMTP に冪等キーが無い以上ここは避けられないので、
// **重複を許して取りこぼしを許さない**側を選んでいる。
// 問い合わせが届かないほうが、2 通届くより悪い。
//
// 【ORDER BY を索引と揃える】
// contact_pending_idx が (next_attempt_at, id) WHERE status = 'pending' なので、
// 同じ並びにすると索引をそのまま辿れる。id を第 2 キーに置くのは
// next_attempt_at が一意でないため。
func (q *Queries) ClaimPendingContacts(ctx context.Context, arg ClaimPendingContactsParams) ([]ClaimPendingContactsRow, error) {
	rows, err := q.db.Query(ctx, claimPendingContacts, arg.Lease, arg.BatchSize)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ClaimPendingContactsRow{}
	for rows.Next() {
		var i ClaimPendingContactsRow
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.Name,
			&i.Email,
			&i.Subject,
			&i.Body,
			&i.AttemptCount,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const countRecentContacts = `-- name: CountRecentContacts :one
SELECT count(*) FROM contact_messages
WHERE client_ip = $1
  AND created_at > now() - $2::interval
`

type CountRecentContactsParams struct {
	ClientIp *netip.Addr
	Window   pgtype.Interval
}

// 同じ IP からの直近の件数を数える (ADR 0008 決定 4 のレート制限)。
//
// 【なぜ専用のカウンタストアを置かないか】
// **アプリのメモリで数えると、複数インスタンスでは効かない。**
// ECS のタスクが N 個あれば実質 N 倍まで通る。閲覧数バッファ (ADR 0006) と
// 同じ構造の問題だが、閲覧数は多少ずれてよいのに対し
// **レート制限はずれると意味を失う**。
//
// 問い合わせは 1 日数十件を想定しているので、
// **contact_messages をそのまま数えれば足りる。**
// contact_rate_limit_idx (client_ip, created_at) で引く。
//
// **この方法が成立するのは問い合わせだからで、コメント投稿には使えない。**
// 毎リクエストで集計クエリを走らせることになる。その段階になったら
// WAF のレート制限ルールか Redis での集計を使う。
//
// **client_ip が NULL の行は数に入らない。** 等値比較は NULL に一致しない
// ため、消し込み済みの古い行は自動的に対象外になる。
// 窓 (既定 1 時間) より古い行しか消し込まないので、実害は無い。
func (q *Queries) CountRecentContacts(ctx context.Context, arg CountRecentContactsParams) (int64, error) {
	row := q.db.QueryRow(ctx, countRecentContacts, arg.ClientIp, arg.Window)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const createContactMessage = `-- name: CreateContactMessage :one

INSERT INTO contact_messages (user_id, name, email, subject, body, client_ip)
VALUES ($1, $2, $3, $4, $5, $6)
RETURNING id, created_at
`

type CreateContactMessageParams struct {
	UserID   *int64
	Name     string
	Email    string
	Subject  string
	Body     string
	ClientIp *netip.Addr
}

type CreateContactMessageRow struct {
	ID        int64
	CreatedAt time.Time
}

// =============================================================================
// 問い合わせ (docs/adr/0008-contact-and-mail.md)
//
// 【このファイルは 2 種類のクエリに割れている】
//
//	受付側  リクエストの中で走る。CreateContactMessage と CountRecentContacts
//	送信側  定期処理の中で走る。Claim... 以降
//
// 分かれているのが決定 1 (保存と送信を分ける) そのものになる。
// 受付側は外部サービスに触らないので、メールプロバイダが落ちていても
// 問い合わせは受け取れる。
// =============================================================================
// 問い合わせを 1 件保存する。**この時点ではまだ送っていない。**
//
// status は既定の 'pending'、next_attempt_at は既定の now() に任せる。
// 明示的に渡さないのは、**「受け付けたらすぐ送信対象になる」が既定である
// ことをスキーマ側に持たせる**ため。ここで時刻を計算すると、
// アプリの時計と DB の時計のずれが送信の遅延として出る。
//
// **id を返すが、外には出さない。** ログ (contact_id) と
// 送信側の追跡に使う内部の識別子で、応答には含めない
// (ADR 0003 未決 #11 / OpenAPI の ContactAccepted を参照)。
func (q *Queries) CreateContactMessage(ctx context.Context, arg CreateContactMessageParams) (CreateContactMessageRow, error) {
	row := q.db.QueryRow(ctx, createContactMessage,
		arg.UserID,
		arg.Name,
		arg.Email,
		arg.Subject,
		arg.Body,
		arg.ClientIp,
	)
	var i CreateContactMessageRow
	err := row.Scan(&i.ID, &i.CreatedAt)
	return i, err
}

const failContact = `-- name: FailContact :execrows
UPDATE contact_messages
SET status     = 'failed',
    last_error = $1
WHERE id = $2
  AND status = 'pending'
`

type FailContactParams struct {
	LastError *string
	ID        int64
}

// 試行回数の上限を超えた行を打ち切る (ADR 0008 決定 1)。
//
// **無限にリトライしない。** 送信できないメールが延々とプロバイダを
// 叩き続けることになり、まともなメールの送達にも響く。
//
// 行は消さない。「受け付けたが送れなかった問い合わせ」が残らないと、
// 運用が手で拾い直すこともできなくなる。
func (q *Queries) FailContact(ctx context.Context, arg FailContactParams) (int64, error) {
	result, err := q.db.Exec(ctx, failContact, arg.LastError, arg.ID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const markContactSent = `-- name: MarkContactSent :execrows
UPDATE contact_messages
SET status     = 'sent',
    sent_at    = now(),
    last_error = NULL
WHERE id = $1
  AND status = 'pending'
`

// 送信できた行を確定する。
//
// **status = 'pending' を条件に含める。** 含めないと、リース切れで
// 2 つのワーカーが同じ行を送ったときに、後から来たほうが
// sent_at を上書きして「いつ送ったか」がずれる。
//
// last_error を消すのは、前回の失敗理由が残っていると
// 「送信済みなのにエラーが出ている行」に見えるため。
//
// CHECK 制約 contact_sent_complete が status と sent_at の対応を要求する。
func (q *Queries) MarkContactSent(ctx context.Context, id int64) (int64, error) {
	result, err := q.db.Exec(ctx, markContactSent, id)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const oldestPendingContact = `-- name: OldestPendingContact :one
SELECT created_at FROM contact_messages
WHERE status = 'pending'
ORDER BY created_at
LIMIT 1
`

// 最も古い未送信の問い合わせの受付時刻。**メトリクス用。**
//
// ADR 0008 の「引き受けるコスト」が挙げているのがこれになる ——
// ワーカーが止まっていることに気づく仕組みが無いと、
// **問い合わせが静かに溜まり続ける**。送信が回っていれば
// この値は常に数分以内で、止まると単調に古くなる。
//
// 0 行なら未送信が無い (= 正常)。
//
// **created_at で並べる。** contact_pending_idx は
// (next_attempt_at, id) なので並べ替えが要るが、pending の集合は
// 送信が追いついていれば常に小さい。止まっているときは大きくなるが、
// そのときは既に検知したい状態そのものなので、遅くて構わない。
func (q *Queries) OldestPendingContact(ctx context.Context) (time.Time, error) {
	row := q.db.QueryRow(ctx, oldestPendingContact)
	var created_at time.Time
	err := row.Scan(&created_at)
	return created_at, err
}

const rescheduleContact = `-- name: RescheduleContact :execrows
UPDATE contact_messages
SET next_attempt_at = now() + $1::interval,
    last_error      = $2
WHERE id = $3
  AND status = 'pending'
`

type RescheduleContactParams struct {
	Backoff   pgtype.Interval
	LastError *string
	ID        int64
}

// 送信に失敗した行を、次の試行へ回す。
//
// **attempt_count はここで増やさない** —— 確保 (ClaimPendingContacts) が
// 既に増やしている。両方で増やすと、1 回の失敗で 2 回ぶん減る。
//
// next_attempt_at はリース (確保時に置いた仮の時刻) を、
// 指数バックオフで計算し直した時刻で上書きする。
// 待ち時間を DB に持つので、**プロセスが落ちても待ちは失われない**
// (Phase 2 のプロセス内リトライとの違い)。
func (q *Queries) RescheduleContact(ctx context.Context, arg RescheduleContactParams) (int64, error) {
	result, err := q.db.Exec(ctx, rescheduleContact, arg.Backoff, arg.LastError, arg.ID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const scrubContactClientIPs = `-- name: ScrubContactClientIPs :execrows
UPDATE contact_messages
SET client_ip = NULL
WHERE id IN (
    SELECT id FROM contact_messages
    WHERE client_ip IS NOT NULL
      AND created_at <= now() - $1::interval
    ORDER BY created_at
    LIMIT $2
)
`

type ScrubContactClientIPsParams struct {
	Retention pgtype.Interval
	MaxRows   int32
}

// 古い行から client_ip を消す。
//
// **個人データを無期限に持たない** (ADR 0008「client_ip は個人データに
// あたるため、無期限には保持しない」)。保持期間はログ (ADR 0010 決定 6 の
// 400 日) と揃える —— 同じ IP がログ側にも出ているので、
// 片方だけ短くしても消えたことにならない。
//
// **行は消さない。** 消すと問い合わせの記録そのものが消える。
// 消すのは列の値だけで、レート制限は窓 (既定 1 時間) の中でしか
// 数えないため、古い行の値が無くても影響しない。
//
// 一度に更新する件数を制限しているのは、期限切れキーの削除と同じ理由。
// 呼び出し側は「0 行になるまで繰り返す」形で使う。
//
// contact_ip_scrub_idx (created_at) WHERE client_ip IS NOT NULL で引く。
// 述語に client_ip IS NOT NULL が入っているので、
// **消し込みが進むほど索引が小さくなる。**
func (q *Queries) ScrubContactClientIPs(ctx context.Context, arg ScrubContactClientIPsParams) (int64, error) {
	result, err := q.db.Exec(ctx, scrubContactClientIPs, arg.Retention, arg.MaxRows)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}
